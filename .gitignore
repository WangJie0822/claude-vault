__pycache__/
*.pyc
.pytest_cache/
*.jsonl
summarized-sessions.json
config.json
*.log
docs/superpowers/
packaging/
# SDD 流程产物本该写在 `.superpowers/sdd/`（已整体 gitignore）下；这两类文件名一旦
# 误落进分发目录（如 `skills/**`）也一律不追踪——F1（整分支终审，2026-09-02）实测
# `skills/vault-loader/task-3-report.md` 曾被意外 `git add` 并随插件分发。
task-*-report.md
test-output*.txt
# subagent-driven 开发的 task 简报 / 多维评审草稿：本地开发产物，含源机路径与私人引用。
# CLAUDE.md 早已声明二者不分发，但此前 `.superpowers/` 实际未被任何 gitignore 规则覆盖
# （唯一保护是 superpowers 工具自己写的 `.superpowers/sdd/.gitignore`，只覆盖一层），
# `.full-review/` 则只被 per-clone 的 `.git/info/exclude` 挡着、新 clone 完全没有。
.superpowers/
# 带 `*` 是因为归档会话会落成 `.full-review-archive-<日期>/` 这样的兄弟目录。
.full-review*/
# 本地调试与评审的临时产物：`pytest --basetemp` 的落点、探针脚本、评审草稿等。
# 不忽略它会让**工作树永远不干净**，进而使 `run_gates.py` 里那道要求干净树的
# 「Codex 产物构建」门禁**永久跳过**——而跳过在汇总表里渲染成 `PASS`，与「跑过并
# 通过」不可区分。2026-09-07 实测：`git status -uall` 的 670 项里 663 项来自这里，
# 该门禁因此从未真正执行过，且本仓库无任何 CI，没有第二个机制会发现。
.tmp/
# git worktree 的物理目录（EnterWorktree 默认落在这里）：纯运行时产物，且里面是另一份
# 完整工作树，误 add 会把整个仓库副本连同其 .full-review/.superpowers 一起提交进来。
.claude/
