# Changelog

本文件记录 claude-vault 的用户可见变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.9.1] - 2026-08-26

这一版修的是上一版**自己**的问题：0.9.0 号称把召回质量变得「可以测量」，但两条主要通道装上去之后从没真正工作过。都是静默失效——功能在，测试全绿，数据是空的。

**开了 `metrics.enabled` 的话，这一版值得升**：`--review` 的精度标注这才第一次真正可用（此前恒返回空候选），而且现在会显示「这篇笔记被召回时你问的是什么」——没有那个上下文，标注根本无从判断。升上来之后建议跑一次 `--reset-counts`（原因见下）。

### 修复

- **`--review` 的精度标注从上线起一条候选都产不出。** 0.9.0 用 `promptSource` 判断提问是否人类发起，只把 `typed`/`queued`/`suggestion_accepted` 放进抽样池。问题是 **hook 的 stdin 根本不下发这个字段**（Claude Code 2.1.220 实证：`UserPromptSubmit` 的 payload 里没有它，它只存在于 transcript），于是落盘的 `src` 恒为空串，白名单一条都放不过。判据现在翻转成黑名单——只排除**确知**是自动化的来源（`sdk`/`system`），缺失与空串按人类输入处理。**开了 metrics 的话，这个版本值得升**：`--review` 的精度侧这才第一次真正可用。
- **near-miss 提示报的是「这篇早就给过你了」，而不是「这篇一直召不回」。** 榜单与 nudge 计数把「本会话已注入过、因此被刻意去重」的笔记也算作擦肩而过。实测：达到提示阈值的 211 篇笔记里 **78.2% 其实已被注入过**，43.6% 甚至被全文注入过——提示的语义整个是反的。根因不只是消费侧漏了过滤，更是**过滤时机**：记录落盘时先按相关度取前 10 条，而被去重的条目相关度结构性更高（它们是过了闸门才被去重的），于是 39% 的轮次里「真擦肩」一条都进不了样本。判据因此下沉到了生成侧。
- **提示还会指向压根不相关的笔记。** 「够不着闸门」这批的相关度下界是 0，实测中位数只有 2.0（闸门是 4），而提示文案写的是「反复接近召回闸门，检查这些笔记的 tags/keywords」——对一篇相关度为 0 的笔记，这两句都是错的指引。现在加了相关度下限 3.0 再计数。

### 新增

- **`--review` 现在会显示「这篇笔记被召回时，你问的是什么」。** 此前它只显示「被召回 63 次、topical≤10.8、路径」，而「该不该被召回」这个问题在不知道当时提问内容时根本无法回答——标注等于掷骰子。现在落盘一个 `prompt_h`（提问原文的加盐 hash，与既有的关键词 hash 同一安全模型），标注时按 session 找到本机 transcript、用 hash **精确匹配**回原文展示。
  - **隐私边界完全不变**：提问原文仍然绝不落盘，transcript 本来就在你本机、本来就存着全文，标注时只读不写。也刻意不落 `transcript_path`（那个路径含项目目录名，而 cwd 特意只存了 hash）。
  - **只对本版之后的新记录生效**：旧记录没有这个定位键，标注时会显示「无提问上下文」。想让标注有意义，需要先正常使用几天积累新数据。
- **`--reset-counts`**：只清 near-miss 累计计数与提示冷却，不碰事件记录与人工标注。需要它是因为计数只增不减：判据改了也只影响此后的累加，存量污染不会自行消退。与 `--purge` 互斥，避免误用那个会删掉不可再生标注的命令。
- **报表新增注入正文字符数**，回答此前答不出的那半个问题——报表一直只统计「召回了什么」，不统计「花了多少 context」。
- 落盘新增 `near_miss_scorelow`（加性可选字段，**不影响历史记录**）。

### 变更

- **报表口径订正。** 「累计入选 N 篇」曾被读成「注入了这么多篇」，它其实是**候选池**累计（实测均 73 篇/轮、中位数 62），而真正渲染给你的只有 `max_notes` 篇（默认 3，**全文那篇计入其中**）；现已改为「候选池累计」并按记录里落盘的配置真值标出渲染篇数。全文注入率的分母也从「全部事件」收敛到「走到打分的轮次」——闸门早退的轮次压根没走到全文判定，计进分母会把 48.6% 稀释成 36.2%。
- near-miss 的两个榜单（「真·擦肩」与「对照·被去重抑制」）现在跑在**同一批记录**上。此前后者跨新旧记录一起计，而前者按设计排除旧记录——两栏并排、表头写着「对照」，读者会拿计数直接相比，那是 apples-to-oranges。
- 报表的注入量标注为「**UPS** 注入正文」并注明不含 SessionStart 通道（后者不落 metrics），空库时直接说「无数据」而不再打出暗示「有数据只是太旧」的解释语。
- **`.salt` 的保密承诺改为按平台如实表述。** 它是加盐 hash 安全性的前提，但 POSIX 上靠 `0600`、**Windows 上没有强制手段**（NTFS 走 ACL，`chmod` 不生效），实际可读范围取决于 `~/.claude` 继承的 ACL。此前文档一句「该文件须保密」在 Windows 上是兑现不了的承诺。介意的话就别在 Windows 上开 metrics。
- **记录体积数字重测。** README 此前写的「单条 3.7~5.9 KB / 90 天 11~16 MB」低估约 1.5 倍；1058 条真实记录实测：完整记录中位 5.9 KB、闸门早退记录 166 B、混合均值 4.0 KB/条，作者本机 66 条/天 ⇒ 90 天约 23 MiB。新增的 `near_miss_scorelow` 因受相关度下限约束、80% 的轮次为空，体积增量中位数只有 +82 B（+1.4%）。

### 内部（不影响使用，但记下来免得下次再犯）

- 首版修复曾在四处文档与 commit message 里声明「三个消费者一律走同一个判据函数」，**实际只有一个在用**，另两个各自内联、双双漏掉相关度下限。根因是那个抽象只返回 `list[str]`，而榜单要计数、抽样池要相关度——两者都拿不到需要的形态，于是各写各的。**抽象没被采用时，先怀疑它的形态。** 现已把判据下沉到落盘那一侧（磁盘上不存在低于下限的条目，无从分叉），并补了一条同时比对三个消费者输出的守卫。
- 落盘新增 `scorelow_floor` 与 `max_notes` 两个自描述字段（均为加性可选，不影响历史记录）：分析层不读配置文件，此前把默认值硬编码进报表文案与抽样逻辑，用户一改配置就开始说假话。
- 相关度下限下沉到生成侧的**代价**：低于下限的条目从此不落盘，将来想调低这个阈值或分析低分分布就没有历史数据。生效值随记录落盘算是部分补偿。
- **意外的净收益：开着 metrics 时这一版反而更快。** 判据收紧后 30%~69% 的轮次不再有任何 near-miss 条目要计数，于是整套「读 45 KB 计数文件 → 排序 → 回写」被跳过（早退发生在建目录与取锁之前），实测每轮省下 0.3~1.0 ms，比新增字段带来的 +17 μs 大两个数量级。
- 报表新增**来源分布**一行（仅在出现非空值时显示）。这是给那个黑名单判据配的自观测出口：它默认放行，若 Claude Code 日后开始下发新的自动化来源值，那些轮次会静默进入人工标注池——打出来至少让这件事第一时间可见。上一次白名单判据空转整整 8 天没被发现，正是因为没人看得见这个字段的真实取值。
- 单条坏记录不再让整个 `--report` 罢工。此前某个数值字段被手工改坏（或磁盘位翻转）会让命令直接抛栈退出、stdout 全空，同文件里的合法记录连同上千条历史一起拿不到——而 `--report` 恰恰是排障入口。现在坏字段按缺失处理、经 stderr 报出条数，其余照常统计。
- schema 版本过滤从「严格相等」改为「受支持集合」，不匹配时报出丢弃条数而不是静默跳过。今天行为完全不变（集合只有当前版本），但此前那种写法让「升级 schema」的代价固定成「静默丢掉全部历史」，等于把版本号变成了永远不能动的死值。

## [0.9.0] - 2026-08-17

这一版把召回质量从「只能凭感觉」变成「可以测量」。**如果你开了 `metrics.enabled`，这个版本值得升**——此前报表能告诉你召回了多少篇，但回答不了「召回得对不对」。最后那条 `files()` 修复与 metrics 无关，对所有 Windows 用户生效。

### 新增

- **`--review` 现在能标注精度，不只是漏召回。** 此前人工标注只从 near-miss（擦肩而过的笔记）抽样，也就是只能回答「这篇该不该被召回」；现在同时从**已经注入给模型的内容**抽样——全文注入的主候选与清单里的 top-3——回答「这次召回得对不对」。标注记录新增 `kind` 字段区分三类（`near_miss` / `admitted_fulltext` / `admitted_list`），全文与清单分开是因为代价差一个量级（全文单篇上限 8192 字节，清单只有一行摘要），混成一类之后就分不出是哪边错了。精度侧只从人类发起的提问里抽样，自动化触发的轮次不进抽样池。**⚠️ 订正：这条当时没有生效——判据依赖的 `promptSource` 字段不在 hook stdin 里，实际结果是一条都不抽。见 [Unreleased]。**
- **near-miss 榜现在能看出成因。** 落盘新增 `dedup` 字段，报表按成因分桶。此前榜首经常是「本会话早就成功注入过、因此被刻意去重」的笔记，它们和「分数不够被拒」混在一起，让人误以为某篇一直召不回来——实测这类占 6.7%（下界）。
- **被闸门拦下的轮次现在也留痕。** vault 不可达、来源被跳过、关键词不足、缓存为空这四种早退，此前完全不落盘，于是报表里的闸门分布恒等于「全部通过」——不是真的全通过，是被拦的从来没被记录。现在各留一条只含时间戳与拦截原因的极简记录，**不含关键词、不含路径**，零隐私增量。
- **记录带上来源。** 新增 `src` 字段（来自 hook 的 `promptSource`），可以把召回按「人类提问 / slash 命令 / SDK 自动化」拆开统计。**⚠️ 订正：该字段实测恒为空——`promptSource` 不在 UserPromptSubmit 的 hook stdin payload 里，按来源拆分统计当时并不可用。字段本身保留作探针（harness 日后下发即自动有值），但消费侧判据已翻转，见 [Unreleased]。**

### 修复

- **Windows 上 `files()` 降级会返回反斜杠路径。** 没装 Obsidian CLI 时会降级到文件遍历，而降级那支返回 `sub\b.md`、CLI 那支返回 `sub/b.md`——同一个 API 两种分隔符，降级不再对调用方透明。Obsidian 的 wikilink 与 Vault 相对路径约定都是正斜杠，现在统一。**此修复不需要开 metrics，影响所有 Windows 用户。**

### 破坏性变更

- **`load_annotations()` 的返回形状变了**：`{path: verdict}` → `{(kind, path): verdict}`。只影响直接调用该函数的代码；`--review` / `--report` / `--purge` 等 CLI 用法不受影响。缺 `kind` 的历史标注一律按 `near_miss` 读取，**已有标注不会丢**。
- **metrics 落盘的磁盘足迹变大**：早退轮次此前完全不写，现在每次写一条极简记录。仍受 `metrics.enabled` 开关约束，未开启时零足迹不变。

### 背景

上一版补的是「笔记写进来时有没有检索词」，这一版补的是「召回出去之后对不对」。两者之间隔着一个盲区：报表里所有数字都只统计**通过了闸门**的轮次，被拦掉的那些既不落盘也不计数，于是「这周有多少次召回被自己的闸门挡掉了」这个问题在数据上无法回答。同样地，人工标注通道只对着 near-miss，「注入了不相关的笔记」这类问题此前完全不可见——而它恰恰是最常见的那一类。

`_metrics.SCHEMA` 保持 `1` 不变，本版新增的全是可选字段。历史记录照常参与统计，缺字段的一律归入「未知（旧记录）」桶，与真实的空值分开计数。

## [0.8.0] - 2026-08-10

堵住一个一直在扩大的召回空洞：**经 `pending-docs.json` 归集进 Vault 的 spec/plan，`keywords` 字段一直是空的**——而 `keywords` 是召回打分里权重最高的精确信号，缺了就意味着那篇笔记在你提问时召不回来。

**如果你用文档归集功能，这个版本值得升。**

### 新增

- **归集时透传 `keywords`。** `pending-docs.json` 的条目现在多认一个可选字段 `keywords`（字符串数组，3-8 个检索扩展词），归集时经校验写进 Vault 副本的 frontmatter。走的是与 `summary`←`context` 完全相同的通路，**不产生任何额外模型调用**——写 `context` 时本来就在写。字段契约见 `references/doc-collection.md`。
- **`/summarize-session` 收尾时自动补缺口。** 新增 `keywords_gap.py`（`--list` 列缺口 / `--set` 写回），接在索引重建之后：会话里的模型读笔记、自己生成扩展词再写回，同样零额外调用。默认每次最多补 10 篇，剩余的下次继续。缺口判定与健康报告里的 `keywords_missing` 复用同一份代码，不会出现「报告说缺 5 篇、实际只补了 3 篇」的偏差。

### 修复

- **补完 `keywords` 却不生效。** 索引重建用「秒级时间戳」判断文件是否需要重读，而「补全 → 重建」几乎总在同一秒内完成，于是重建会认为文件没变、跳过它，缓存里始终是空的 `keywords`。由于召回读的是缓存，**补了等于没补**，且下次会话还会把同一篇再列一遍。现在写回后直接同步缓存条目。同一问题也影响 `enrich_keywords.py`，只是它每篇要调一次模型、耗时跨秒，不易触发。

### 背景

这个缺口的成因是**速率不匹配**：归集是自动的（每次 `/summarize-session` 都跑），而补 `keywords` 此前只有 `enrich_keywords.py` 一条路——付费、手动、需要显式 opt-in。于是每归集一篇就多一个空洞，只能靠人想起来跑一次批量回填才清一次。

作者本机实测：最后一次回填之后 12 天累积 37 篇，覆盖率从 99% 掉到 96%，且这 37 篇**全部**是归集进来的 spec/plan——非归集笔记的覆盖率是 100%。本版把源头（归集透传）和兜底（流程内补全）都补上，缺口不再随时间增长。

## [0.7.1] - 2026-08-08

对 0.7.0 新增的效果评估机制做了一轮完整评审并整改。**如果你开了 `metrics.enabled`，这个版本值得升**——下面每一条在 0.7.0 里都会让你拿到错的数字、丢数据，或者让 CLI 直接崩掉。

### 修复

- **人工标注被算进召回成功率统计。** `annotations.jsonl` 与事件记录同在一个目录，`--report` 的扫描把它一并读了进去；标注记录没有 `gate` 字段，被默认成「成功召回」。结果是你标注得越多，报表里的成功率越好看。
- **`--purge --review` 同传时 purge 静默胜出。** 两个动作靠代码里的书写顺序定优先级，而 purge 在前——你以为要去逐条标注，实际把包括人工标注在内的全部数据删了，全程没有任何提示。现在同传会直接报错退出。
- **`--review` 会因一条损坏记录整个崩掉。** 标注文件里 `path` 若不是字符串（手工编辑或磁盘异常所致），读取时抛出未捕获的异常，已存的标注一条都读不出来。
- **承诺的「超期自动清理」从未执行。** `retention_days` 的清理函数在生产代码里没有任何调用点，文档却写着会自动清理——数据实际会无限累积。现在按「每天至多一次」的频率闸门接线。
- **near-miss 提示会静默失效。** 跨会话共享的计数文件是无保护的读-改-写，并发下丢更新（实测 4 进程各 +15、期望 60 实际只落 3）。计数永远攒不到阈值，提示就永远不出现，且没有任何征兆。
- **`--report` / `--review` 随数据量单调恶化。** 旧实现把全部历史记录一次性读进内存（实测 105MB 数据耗时 7 秒、峰值内存 574MB）。现在改为流式逐行处理。
- **单条记录可能写出远超预期的体积。** 被召回笔记列表未截断，实测单条记录达 29962 字节、约为文档声称上界的 9 倍。现在按分数截断，但统计口径取自截断**之前**，报表数字不受影响。
- **会话与提问标识可被静默对调。** 两个相邻的同类型参数按位置传入，写反了不会报错，只会让落盘数据错位。现在改为强制具名传参。

### 其他

- 文档中若干与实现不符的描述已订正（保留期约束范围、纯函数与副作用的界定、报表路径隐去所用的哈希方式等）。

## [0.7.0] - 2026-08-06

本轮两件事：修掉两个**让召回质量被误判**的缺陷，以及新增一套**默认关闭**的效果评估机制，用于回答「知识库到底有没有帮上忙」。

### 新增

- **效果评估机制（`metrics`，opt-in，默认关闭）。** 开启后每次提问会记录一条决策面数据：哪些笔记进了候选、哪些擦肩而过、闸门怎么判的。用于长期评估召回效果、指导阈值调优。
  - **数据只留本机，不上传任何地方。**
  - 隐私收敛：提问的关键词**只存加盐哈希**，被召回笔记的命中词与相对路径以明文存放（供你人工复核候选是否真的相关），**提问原文绝不落盘**。
  - 落盘发生在全部五道停用闸门**之后**——`opt_out_paths` 等排除设置照常生效。
  - 每会话一个独立文件，超出保留期的自动清理。
- **`analyze_metrics.py` 分析 CLI。** `--report` 出报表（默认隐去路径，只显示稳定短 ID）；`--review` 对「够相关但没入选」的候选做抽样人工标注；`--purge` 一键清空全部数据。
- **near-miss 提示。** 某篇笔记在一次会话里反复「够相关但没入选」达到阈值时，给一次提示——这通常意味着阈值需要调整。带一周全局冷却，不会刷屏。

### 修复

- **最强信号反而得分最低。** 一篇笔记的标签和关键词**同时**命中提问时，旧逻辑会把两次命中当成重复只算一次——于是「命中得越全、排得越靠后」。这是个惩罚性反转，直接影响每一次召回的排序。
- **摘要里的「关键词」标签会误导对召回质量的判断。** 那一行展示的其实是提问里的全部关键词碎片（含没有命中任何笔记的），却标着「关键词」，看上去像是「这些词都命中了」。现已改为只展示真正命中的词。

## [0.6.0] - 2026-08-05

本轮主线是**让静默失效变成可见**：知识库不工作时，此前几乎没有任何征兆——config 损坏、索引损坏、vault 路径写错、开关没生效，表现全都是「怎么没有注入了」。这个版本给这些情形补上了诊断通道与自检入口，并修掉了若干会让 hook 直接静默死亡的缺陷。

### ⚠️ 行为变更（升级前请读）

- **`opt_out_paths` 现在会真正生效，某些目录可能突然停止注入。**

  此前它用裸字符串前缀比对，于是同一个目录的多种合理写法只有一部分有效：正斜杠写法（`"D:/work/secret"`，而 JSON 里写反斜杠需要双写，用户自然会写正斜杠）、大小写不同、末尾多一个分隔符——**全都不生效**。如果你配过 opt-out 却发现该目录仍在注入，那不是错觉。

  现在改为路径归一化（`expanduser` + `resolve` + 大小写归一）加边界锚定。副作用是：**你此前"写了但没生效"的配置，升级后会开始生效**。如果某个项目升级后不再注入，先检查 `opt_out_paths` 是不是本来就想排除它。

  同一处修复还消除了反向的误伤：配置 `.../secret` 不再把兄弟目录 `.../secret-public` 一起拦掉。

- **`~/.claude/skills/vault-loader/config.json` 首次运行不再被全量物化。**

  旧版首跑会把整份 `DEFAULT_CONFIG` 写进 config，于是此后每次默认值演进都被这份快照压制、对存量用户无效。现在只写最小占位。**存量用户仍受旧残留影响**——用 `migrate_config.py` 检测与清理，见下方「新增」与 [docs/MIGRATION.md](docs/MIGRATION.md)。

### 新增

- **失效诊断通道**：config 损坏、vault 路径不存在、索引损坏、跨 skill 路径不一致等情形，现在会经 `systemMessage` 给出用户可见提示，而不只是写一行没人看的 stderr。按诊断类型分别做 TTL 冷却，不会每次提问都刷屏。
- **`migrate_config.py --doctor`**：只读健康自检，一次性列出 config 状态、vault 路径与可达性、索引状态与条目数、跨 skill 路径一致性、以及**旧版默认值残留计数与键名**。不写盘、不 dump 用户配置原文（输出可安全贴进 issue）。
- **索引状态区分**：内部把「空索引」的五种成因（不存在 / 空 / 版本不符 / 损坏 / 超大）分开，只有真失效才告警——新装用户和 cache 版本升级期间不会收到误报。
- **注入头只展示真正命中的查询词**：此前会把全量 prompt 关键词（含未命中任何展示笔记的碎片）塞进注入头。

### 修复

- **索引读取的五类畸形输入**（任一都可能让整份索引作废，其中两类会让 hook 静默死亡、stdout 全空、连诊断都不触发）：
  - JSON 根不是对象（`[]` / `null` / 字符串都是合法 JSON）
  - 深嵌套 JSON 触发 `RecursionError`（约 80KB 即可触发，远在 10MB 上限之内）
  - `summary` / `path` 无长度上限，单字段可逼近 10MB 进入模型上下文
  - `tags` 写成标量字符串时被逐字符迭代成假 tag，污染 tag-IDF 统计
  - 一条笔记的 `mtime` 非法会让**整份索引**判损坏——3 篇里坏 1 篇，存活 0 条
- **笔记标题可伪造终端界面**：摘要清单里的标题直接取自索引的 path 键（外部可控，Vault 可能是 clone 来的），未剥控制字符也未折叠换行，可在 `systemMessage` 里伪造出多行内容冒充 Claude Code 的告警并诱导执行命令。现已净化并截断。
- **全文注入的框架分隔符改用随机 nonce**：此前用字面量 `---` 框住不可信正文，笔记正文写 `---` 即可伪造「引用结束」。
- **笔记路径解析收敛到带容器校验的单点**，堵住索引中被篡改的越界 path 读到 Vault 外文件。
- `ensure_vault` 收敛，恢复 vault-loader 对 Vault 只读的不变量。
- 两个 hook 的输出收敛到唯一出口，杜绝一次执行写出两段 JSON 文档（那会让 Claude Code 解析失败并把原始 stdout 整个推进模型上下文）。
- 补上 hook 顶层 import 的 fail-open 缺口——此前导入期异常会直接 exit 1，兜底覆盖不到。

### 文档

- 订正多处失实表述，其中影响最大的一条：**改 Vault 路径必须改两处**。`/summarize-session --set-default` 只写 summarize-session 侧，vault-loader 读的是自己 config 里的 `vault_path`，不会跟着走——此前三份文档都写成「读端从写端取路径」，照做会让自动注入在一个空目录上静默工作。
- README 补充定向回退与收敛脚本之间的冲突提示（`scoring.prompt_keyword_hit: 3` 恰好等于历史默认，会被 `--apply` 当作残留清理）。

### 内部

- 测试从 366 增至 438（vault-loader），新增畸形输入、路径归一、标题伪造、诊断通道等守卫；性能守卫判据从「3 次取最差」改为「7 样本取中位数 + 预热」，消除尖峰主导导致的误判。

## [0.5.0] - 2026-07-22

- tag 命中改按 IDF 加权，`keywords` 命中权重从 3 提到 5。
- 写端补齐 keywords 覆盖，`rebuild_index` 统计覆盖率并在过低时告警。
- 安全：笔记路径解析收敛到带容器校验的单点。
- 存量用户升级须知与 `migrate_config.py` 收敛脚本，见 [docs/MIGRATION.md](docs/MIGRATION.md)。

## [0.4.0] - 2026-07-02

- 中文查询改用 CJK bigram 分词（无词典滑窗 + 函数词停用表），中文问句与短词的召回明显改善。
- 召回池默认排除 `tags` 含 `archived` 的笔记（`/vault` 手动检索不受影响）。
- 安全：清单模式补隔离声明，注入文本净化控制字符（含 Unicode 行分隔符折叠）。

## [0.3.0] - 2026-07-01

- 移除 auto-mode 整套，插件瘦身。
- 新增归档笔记清理（`prune_archived`）。

## [0.2.0] - 2026-06-27

- 笔记 frontmatter 支持 `keywords` 字段作为检索扩展词，参与读端打分。

## [0.1.1] - 2026-06-24

- 修复 SKILL.md 中指向已退役源目录的脚本路径，改用插件 cache 定位器。

## [0.1.0] - 2026-06-23

- 首个插件版本：summarize-session / vault-loader / vault 三个 skill 与 SessionStart、UserPromptSubmit 两个 hook 打包分发。
